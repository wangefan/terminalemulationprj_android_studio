<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">
    <Preference
        android:key="@string/fb_enable_reader_key"
        android:title="@string/fb_enable_reader"/>
    <Preference
        android:key="@string/fb_disable_reader_key"
        android:title="@string/fb_disable_reader"/>
    <Preference
        android:key="@string/fb_reader_sound_key"
        android:title="@string/soundfile_title"/>
    <ListPreference
        android:key="@string/reader_vib_key"
        android:title="@string/vib_title"
        android:entries="@array/vib_arr"
        android:entryValues="@array/vib_arr_vals"
        android:negativeButtonText="@null"
        android:positiveButtonText="@null" />
</PreferenceScreen>
