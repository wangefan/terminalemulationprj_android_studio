<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- NOTE: Hide buttons to simplify the UI. Users can touch outside the dialog to
         dismiss it. -->
    <!-- NOTE: ListPreference's summary should be set to its value by the activity code. -->
    
     <CheckBoxPreference
        android:key="is_ssh_enable"
        android:title="@string/pref_Title_ssh_enable"
        android:defaultValue="false" />
     
     
       
       <EditTextPreference
            android:title="@string/pref_Title_User"
            android:key="prefsshUserNamePrompt"
            android:dependency="is_ssh_enable"
            />
         
       
       <EditTextPreference
            android:title="@string/pref_Title_Password"
            android:key="prefsshPasswordPrompt"
            android:dependency="is_ssh_enable"
            />
       
       
      
     
     

</PreferenceScreen>
